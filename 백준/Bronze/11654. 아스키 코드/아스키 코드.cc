#include <iostream>
#include<string>
#define setting ios::sync_with_stdio(NULL),cin.tie(NULL),cout.tie(NULL)
using namespace std;

int main()
{   
    setting;
    char s;
    cin >> s;
    cout << (int)s;
    return 0;
}
